/**
  Returns the full path to an example
**/
export const examplePath = (name) => `/examples/${name.toLowerCase()}.html`;

/**
  Returns the full path to a reference item
**/
export const referencePath = (name) => `/reference/${name}.html`;
